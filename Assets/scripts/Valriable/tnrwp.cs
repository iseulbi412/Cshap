using UnityEngine;

public class tnrwp : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        int number = 3;
        string result = "홀수";
        Debug.Log($"{number}은{result}입니다.");
    }

}
